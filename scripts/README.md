# Development Scripts

Development-only utilities that support DocuMind engineering workflows.

## Diagnostics

The `diagnostics/` directory contains utilities for inspecting the
Chroma collection, document metadata, and retrieval behavior.

## Maintenance

The `maintenance/` directory contains utilities used to maintain
or migrate evaluation data.

These scripts are not part of the production runtime.